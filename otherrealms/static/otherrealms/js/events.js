// Events scripts

console.log("Loading events script...")

document.addEventListener('DOMContentLoaded', () => {
    
    ready()

})

function ready() {
    console.log("DOM loaded! Ready loaded!")
}